import 'mocha';

describe('Save three blocks', () => {

  it('should contain three blocks', () => {
    //expect("Hello world!").to.equal('Hello world!');
  });

  it('should contain 7 addresses', () => {
    //expect("Hello world!").to.equal('Hello world!');
  });

  it('should contain 2 address clusters', () => {
    //expect("Hello world!").to.equal('Hello world!');
  });

  it('first cluster should have a balance of 100', () => {
    //expect("Hello world!").to.equal('Hello world!');
  });

  it('second cluster should have a balance of 50', () => {
    //expect("Hello world!").to.equal('Hello world!');
  });

});